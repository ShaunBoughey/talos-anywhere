[defaults]
host_key_checking = False
inventory = inventory.ini
# Increase timeout for VPS network latency
timeout = 30

[ssh_connection]
# Speed up connections
pipelining = True
ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o StrictHostKeyChecking=no
